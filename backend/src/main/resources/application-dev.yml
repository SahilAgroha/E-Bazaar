spring:
  datasource:
    # Uses local values but allows override via .env if needed
    url: ${DB_URL:jdbc:mysql://localhost:3306/buyBaazar}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  # Email configuration for local testing
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USER:sahilsheo444@gmail.com}
    password: ${MAIL_PASS:sujm gufu hncq kchf}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000}
  razorpay:
    api-key: ${RAZORPAY_KEY}
    api-secret: ${RAZORPAY_SECRET}
  stripe:
    secret-key: ${STRIPE_SECRET}

server:
  port: 8080 # Matches your patterns from the patterns provided earlier